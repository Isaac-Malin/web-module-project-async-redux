const initialState = {
  activity: 'Learn to play a new instrument',
  type: 'music',
  price: '0.55'
}

export const reducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state
  }
}